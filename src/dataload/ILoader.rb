require_relative "../datamodel/LineBlock";

# Defines the basic API for loading files
class ILoader
    # Handles files and loads to the line_blocks list
    #
    # @param filepath -> The file path to read from
    # @param line_blocks -> The list to fill
    def load(filepath, line_blocks) = self;
end
